<%= form_for(@best_user) do |f| %>
  <% if @best_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@best_user.errors.count, "error") %> prohibited this best_user from being saved:</h2>

      <ul>
      <% @best_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
