<h1>Listing best_users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @best_users.each do |best_user| %>
      <tr>
        <td><%= best_user.email %></td>
        <td><%= link_to 'Show', best_user %></td>
        <td><%= link_to 'Edit', edit_best_user_path(best_user) %></td>
        <td><%= link_to 'Destroy', best_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Best user', new_best_user_path %>
